export class PollingOptions {
  /**
   * Specifies the day to start polling for a bill.
   */
  startTryingOn = 14;

  /**
   * Specifies the day to stop polling for a bill.
   */
  stopTryingOn = 20;
}
